#Код используется для компиляции двух файлов .c в исполняемый файл usehotel.exe с помощью утилиты mingw32-make.
#.PHONY: all clean #Директива ".PHONY" объявляет целевые имена, которые не являются фактическими файлами, но всегда выполняются. 
#В данном случае, "all" и "clean" -- это целевые имена.

all: usehotel #all: - это метацель, которая вызывает цели usehotel.

usehotel: usehotel.o hotel.o    #usehotel: - это цель, зависящая от файлов usehotel.o и hotel.o, которые будут скомпилированы в последствии.
#Используется для создания объектных файлов из сгенерированных исходных файлов.
	gcc usehotel.o hotel.o -o usehotel.exe
#gcc usehotel.o hotel.o -o usehotel.exe - Команда компилятора gcc,
#использующая два объектных файла usehotel.o и hotel.o для создания выполняемого файла usehotel.exe.
usehotel.o: usehotel.c hotel.h #usehotel.o: - Это цель 'usehotel.o' на которую зависят файлы 'usehotel.c' и 'hotel.h', 
#представленные как аргументы командной строки рядом с опцией -c для компиляции (без линковки) файла 'usehotel.c' в объектный файл 'usehotel.o'.
	gcc -c usehotel.c

hotel.o: hotel.c hotel.h #hotel.o: - Это цель hotel.o, на которую зависят файлы hotel.c и hotel.h, 
#которые компилруются с помощью команды gcc -c hotel.c в объектных файл hotel.o.
	gcc -c hotel.c

#clean:
#    rm -f *.o #Это позволит автоматически удалять все созданные .o файлы при запуске make clean.

#Этот код является примером использования makefile для сборки программного обеспечения из нескольких файлов 
#и может быть выполнен на платформе Windows с установленным MinGW.